<UserControl x:Class="IDE.Common.Views.Browse"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:mui="http://firstfloorsoftware.com/ModernUI"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">


    <Grid x:Name="grid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="97*"/>
            <ColumnDefinition Width="192*"/>
            <ColumnDefinition Width="11*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="108*"/>
            <RowDefinition Height="19*"/>
            <RowDefinition Height="23*"/>
        </Grid.RowDefinitions>


        <DockPanel>
            <TextBlock DockPanel.Dock="Top" Text="Available remote programs:" Style="{StaticResource Heading2}" Margin="0,0,0,8"/>
            <DataGrid ItemsSource="{Binding RemotePrograms}" SelectedItem="{Binding SelectedRemoteProgram}" AutoGenerateColumns="False">

                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Refresh"  Command="{Binding ContextClickCommand}" ToolTip="Refreshes list of available programs." CommandParameter="{Binding DataContext.RemoteProgram, RelativeSource={RelativeSource Mode=Self}}"/>
                        <MenuItem Header="Pull"     Command="" ToolTip="Sends selected program from manipulator to user."/>
                        <MenuItem Header="Execute"  Command="" ToolTip="Starts executing selected program."/>
                    </ContextMenu>
                </DataGrid.ContextMenu>
                
                <DataGrid.Columns>
                    <mui:DataGridTextColumn Header="Name" Binding="{Binding Name, Mode=OneWay}"/>
                    <mui:DataGridTextColumn Header="Size" Binding="{Binding Size, Mode=OneWay}"/>
                    <mui:DataGridTextColumn Header="Last edited" Binding="{Binding Timestamp, Mode=OneWay}"/>
                </DataGrid.Columns>
                
            </DataGrid>
        </DockPanel>

        <!--List-->
        <!--<TextBlock Text="RV-E3J files:" Margin="10,7,0,0" VerticalAlignment="Top" FontWeight="Bold"/>
        <ListView ItemsSource="{Binding RemotePrograms}" Grid.Row="1" SelectedItem="{Binding SelectedRemoteProgram}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>-->

        <!--Buttons for list management-->
        <StackPanel Orientation="Horizontal" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2">
            <Button Command="{Binding RefreshClickCommand}" Width="40" Height="40" Margin="0,5,0,0" ToolTip="Refresh Program List">
                <Button.Background>
                    <VisualBrush>
                        <VisualBrush.Visual>
                            <Grid>
                                <Grid DataContext="{Binding Appearance}" Background="{Binding ThemeColor}"/>
                                <Image Source="/Icons/Refresh.png" Stretch="Uniform"/>
                            </Grid>
                        </VisualBrush.Visual>
                    </VisualBrush>
                </Button.Background>
            </Button>
            <Button Command="{Binding DownloadClickCommand}" Width="40" Height="40" Margin="5,5,0,0" ToolTip="Pull Program From Manipulator">
                <Button.Background>
                    <VisualBrush>
                        <VisualBrush.Visual>
                            <Grid>
                                <Grid DataContext="{Binding Appearance}" Background="{Binding ThemeColor}"/>
                                <Image Source="/Icons/Download.png" Stretch="Uniform"/>
                            </Grid>
                        </VisualBrush.Visual>
                    </VisualBrush>
                </Button.Background>
            </Button>
            <Button Command="{Binding UploadClickCommand}" Width="40" Height="40" Margin="5,5,0,0" ToolTip="Push Program To Manipulator">
                <Button.Background>
                    <VisualBrush>
                        <VisualBrush.Visual>
                            <Grid>
                                <Grid DataContext="{Binding Appearance}" Background="{Binding ThemeColor}"/>
                                <Image Source="/Icons/Upload.png" Stretch="Uniform"/>
                            </Grid>
                        </VisualBrush.Visual>
                    </VisualBrush>
                </Button.Background>
            </Button>
            <Button Command="{Binding RunClickCommand}" Width="40" Height="40" Margin="165,5,0,0" ToolTip="Run Program">
                <Button.Background>
                    <VisualBrush>
                        <VisualBrush.Visual>
                            <Grid>
                                <Grid DataContext="{Binding Appearance}" Background="{Binding ThemeColor}"/>
                                <Image Source="/Icons/Play.png" Stretch="Uniform"/>
                            </Grid>
                        </VisualBrush.Visual>
                    </VisualBrush>
                </Button.Background>
            </Button>
            <Button Command="{Binding StopClickCommand}" Width="40" Height="40" Margin="5,5,0,0" ToolTip="Stop Program">
                <Button.Background>
                    <VisualBrush>
                        <VisualBrush.Visual>
                            <Grid>
                                <Grid DataContext="{Binding Appearance}" Background="{Binding ThemeColor}"/>
                                <Image Source="/Icons/Stop.png" Stretch="Uniform"/>
                            </Grid>
                        </VisualBrush.Visual>
                    </VisualBrush>
                </Button.Background>
            </Button>

        </StackPanel>
        <!--Command window-->
        <Grid Grid.Row="1" Grid.RowSpan="2" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="25"/>
            </Grid.RowDefinitions>

            <ContentControl Content="{Binding CommandHistory}">
                <ContentControl.ContextMenu>
                    <ContextMenu>

                        <MenuItem Header="Font Family">
                            <MenuItem.Icon>
                                <Image Source="/Icons/Delete.png"/>
                            </MenuItem.Icon>
                            <MenuItem Header="Times New Roman" Command="{Binding FontTNRomanCommand}"/>
                            <MenuItem Header="Arial" Command="{Binding FontArialCommand}"/>
                            <MenuItem Header="Calibri" Command="{Binding FontCalibriCommand}"/>
                            <MenuItem Header="Segoe UI" Command="{Binding FontSegoeUICommand}"/>
                        </MenuItem>

                        <Separator/>

                        <MenuItem Header="Export" Command="{Binding ExportHistoryCommand}">
                            <MenuItem.Icon>
                                <Image Source="/Icons/Delete.png"/>
                            </MenuItem.Icon>
                        </MenuItem>

                        <MenuItem Header="Clear" Command="{Binding ClearHistoryCommand}">
                            <MenuItem.Icon>
                                <Image Source="/Icons/Delete.png"/>
                            </MenuItem.Icon>
                        </MenuItem>

                    </ContextMenu>
                </ContentControl.ContextMenu>
            </ContentControl>
            <DockPanel Grid.Row="1">
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                    <Button Content="Send" Command="{Binding SendClickCommand}"  Width="75" HorizontalAlignment="Right" DockPanel.Dock="Right"/>
                    <Controls:ToggleSwitch CheckedCommand="{Binding OnSyntaxCheckCommand}" UnCheckedCommand="{Binding OffSyntaxCheckCommand}" OnLabel="On" OffLabel="Off" MaxWidth="75" Margin="0,-5,0,0"  ThumbIndicatorBrush="#FFC1C1C1" ToolTip="If checked, syntax will be validated." OffSwitchBrush="Red" OnSwitchBrush="Green"/>
                </StackPanel>
                <ContentControl x:Name="CommandInput" Content="{Binding CommandInput}" DockPanel.Dock="Left"/>
            </DockPanel>
        </Grid>


    </Grid>
</UserControl>
